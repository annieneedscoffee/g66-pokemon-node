<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Let's play!</title>
    <style>
      .warning{
        color: red;
      }

        h1, p, h2, h3{
          margin-left: 5%;
        }
        body{
          background-image: url("https://t5.rbxcdn.com/ec3fee5fceaed0c83fa0c799ff3abfd5");
          background-size: cover;
            background-repeat: no-repeat;
        }
        .box{
          float: left;
          border: 1px solid black;
          background-color: white;
        }
      </style>

  </head>
  <body>
    <div class="box">


    <% let moreTwo = false %>
    <% let x = [] %>
    <% let fight = []%>
    <% let winners = []%>
    <% for(y=0;y<pokemon.length;y++){ %>
   <%   if(pokemon[y].in_gym===true){ %>
     <% x.push(pokemon[y].name) %>
     <% fight.push(pokemon[y].cp) %>
     <% winners.push(pokemon[y].id) %>
 <%   } %>
 <% } %>
 <% if(x.length>1){ %>
 <% moreTwo = true} %>
 <% if(moreTwo === false){ %>
<p>Sorry, you need two pokemon in the gym to play. Go back to the gym selection page <a href="/gym">here</a> to select your pokemon to play.</p>
   <% } else if(x[0]===x[1]){ %>
<p class="warning">"A Pokemón cannot fight itself! Please choose a different Pokemón!"</p>
   <% }else{ %>
     <% if(fight[0]>fight[1]){ %>
     <h1>Let's Play! - <%= x[0] %> v. <%= x[1] %></h1>
     <button type="button" name="button"><a href="/battle/<%= winners[0] %>">Battle!</a></button>
     <% }else{ %>
       <h1>Let's Play! - <%= x[0] %> v. <%= x[1] %></h1>
       <button type="button" name="button"><a href="/battle/<%= winners[1] %>">Battle!</a></button>
       <% } %>
     <% } %>
       </div>
  </body>
</html>
